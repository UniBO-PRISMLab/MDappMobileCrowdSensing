import 'package:flutter/material.dart';
import '../controllers/search_places_controller.dart';

class SearchPlacesView extends StatelessWidget {
  const SearchPlacesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SearchPlacesController();
  }
}

